{
  "name": "hive-enforcer",
  "version": "1.0.0",
  "description": "Hook-based enforcement engine for Hive Plugin phase/gate rules",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
