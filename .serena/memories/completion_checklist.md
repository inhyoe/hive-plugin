After changes, run the relevant validation/test commands for the touched area. For top-level plugin changes, prefer `bash scripts/run-tests.sh` plus targeted validators. For dashboard branch changes, run the relevant frontend/server test commands and at least lint/build where applicable. Confirm branch-specific paths exist before making assumptions, since dashboard code currently lives on feat/dashboard rather than main.